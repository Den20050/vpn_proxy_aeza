# ТЗ для Claude Sonnet 4.6

## Проект
Развёртывание устойчивого к блокировкам РКН (ТСПУ) VPN/прокси-сервера на базе VLESS + Reality + Sing-box.

## Цель задачи
Создать полную, пошаговую, production-ready инструкцию и все необходимые файлы для развёртывания Sing-box (VLESS + Reality) на VPS в Нидерландах.

Параметры сервера:
- Тариф: NLs-1
- 1 ядро CPU
- 2 ГБ RAM
- 30 ГБ NVMe
- Ubuntu 22.04

Сервер должен одновременно обслуживать:
- Telegram-бота Silent Couple Bot (aiogram 3 + arq + Redis queue)
- 10-15 обычных пользователей (с возможностью масштабирования до 20-25)

## Требования к решению

### Устойчивость к блокировкам
- Использовать VLESS + Reality с uTLS fingerprint Chrome
- Реализовать маскировку под популярные сайты (рекомендовать несколько вариантов: `microsoft.com`, `apple.com`, `cloudflare.com` и т.д.)
- Использовать порт `443` (TCP + UDP при необходимости)
- Поддержать `xtls-rprx-vision` flow

### Архитектура использования
- Основной inbound: VLESS + Reality для пользователей (отдельный UUID на человека или на небольшую группу)
- Дополнительный inbound: SOCKS5 (локальный, `127.0.0.1:1080`) специально для Telegram-бота (selective routing)
- Бот может быть:
  - полностью перенесён на этот сервер, или
  - оставаться на Timeweb (с маршрутизацией только трафика к `api.telegram.org` и `*.telegram.org`)

### Производительность и ограничения
- Сервер имеет всего 1 ядро и 2 ГБ RAM - решения должны быть максимально лёгкими
- Включить mux (multiplexing) для снижения нагрузки
- Добавить разумные лимиты на соединения
- Учесть пиковую нагрузку утром (07:30) и вечером (21:30), когда бот массово отправляет картинки

## Что нужно подготовить

### Основные файлы
- Полный `config.json` для Sing-box (с комментариями)
- `systemd` service-файл
- Скрипт для генерации ключей, UUID, ссылок и QR-кодов (bash)
- Пример selective routing для бота (через SOCKS5 или policy routing)
- Инструкция по firewall (`ufw`)
- Рекомендации по мониторингу (`htop`, `iftop`, `vnstat`, журналы)

### Дополнительно (по возможности)
- Скрипт автоматического обновления Sing-box
- Рекомендации по смене `short_id` / `server_name` при деградации
- План апгрейда сервера при росте количества пользователей

## Формат вывода
Выдай ответ в виде хорошо структурированного Markdown-документа (как техническую документацию), который можно сразу сохранить как `VPN_Singbox_Deployment_Guide.md`.

Структура документа должна включать:
- Введение и цели
- Предварительные требования
- Пошаговая установка (с командами, которые можно копировать)
- Конфигурационные файлы (с объяснениями каждого параметра)
- Развёртывание и запуск
- Раздача доступа пользователям (генерация ссылок + QR)
- Интеграция с Telegram-ботом
- Мониторинг, обслуживание и безопасность
- Troubleshooting и рекомендации при блокировках
- Список полезных команд

## Важные правила для тебя (Sonnet 4.6)
- Будь максимально точным и production-oriented
- Добавляй комментарии в конфигах
- Учитывай ограничения слабого сервера (1 ядро / 2 ГБ)
- Предлагай несколько вариантов `server_name` и `short_id`
- Укажи, где можно упростить, а где лучше сделать надёжнее
- Используй современные best practices Sing-box 2026 года

Начинай работу. Если нужны уточнения - задавай вопросы в начале.